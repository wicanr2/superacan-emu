# Super A'Can 模擬器開發守則

本儲存庫實作 Super A'Can 主機與晶片的可攜式模擬器。目標是以可回查證據重建
硬體可觀察行為，使商用軟體能在 Linux 上正常執行；這不是遊戲 remake 專案，
不得套用遊戲內容重製、玩法補完或 `dist-all/` 交付規則。

## 權威來源與文件職責

1. 本檔規範本儲存庫的開發方式；`CONTEXT.md` 是目前真相，`WORKLIST.md` 是唯一
   可執行待辦，`WORKLOG.md` 保存歷程，`README.md` 只作穩定入口。
2. `../acan/` 是硬體與 Bcan 逆向知識庫，只能唯讀引用；目前由另一工作階段審查，
   本儲存庫不得代為修改或提交。審查後的新證據須以勘誤方式同步，不能靜默覆蓋。
3. 證據優先順序為：實機量測／晶片文件與原始 ROM、BIOS 可重現行為；Bcan 固定版本
   的反組譯與動態觀察；MAME 固定 commit 的 driver／device 行為；遊戲相容性觀察；
   最後才是類似硬體的類比。來源不同不代表可直接混成同一項事實。
4. `docs/verify-*.md` 保存可重現驗證；反組譯證據仍須遵守主機共用規則的原始位址、
   雜湊、工具版本、位址空間與推論等級契約。

## 模擬器通則

### 晶片與匯流排邊界

- 每顆晶片是有明確暫存器、訊號線、時鐘與內部狀態的裝置。CPU 核心、UMC6650、
  UM6618、UM6619、主機 DMA、輸入與媒體載入應保持清楚邊界；前端不可直接改寫
  晶片內部狀態來讓遊戲通過。
- 位址空間須明列資料寬度、位址寬度、端序、遮罩、鏡射、bank／overlay、未映射
  讀值，以及 8／16 位元存取的副作用。不能把 word 存取任意拆成兩次 byte 存取；
  觸發型暫存器必須依實際 bus transaction 只產生一次副作用。
- 暫存器規格至少記錄 reset 值、可讀／可寫位元、write-one-to-clear 或讀取清除等
  副作用、IRQ／DMA 關係及未解位元。未知位元應保留，不以方便實作的常數吞掉。
- ROM／BIOS／RAM、共享記憶體、動態 bank 與 overlay 分開建模。受版權保護輸入只可
  由使用者提供，絕不加入 Git 或公開產物。

### 時間、排程與中斷

- 所有事件以模擬時間或晶片 cycle 排程，不以主機 wall-clock、SDL callback 次數或
  `sleep` 決定硬體結果。音畫輸出只消費模擬狀態，不反向改變核心時序。
- 多 CPU 比率、掃描線、vblank／raster、timer、DMA 完成與音訊取樣必須共享單調的
  時間基準；累積餘數不可每幀捨去，以免長時間漂移。
- IRQ／NMI／RESET／HALT 必須區分 level、edge、pulse、ack 與優先權。第三方 CPU
  核心的 API 行為要用最小測試確認，不能從函式名稱推定實體訊號語意。
- 為了相容而採用的 HACK、固定延遲或 case 表須靠近程式標明來源與限制，並在
  `WORKLIST.md` 保留替換條件；不得冒稱真實硬體公式。

### 可重現性、除錯與失敗政策

- 同一 ROM／BIOS 雜湊、輸入事件與初始狀態應產生相同 CPU／bus trace、frame hash、
  audio hash 與結束狀態；除錯輸出不得改變排程。
- 每個相容性缺陷先縮成最小可重播案例：固定輸入雜湊、版本、frame／cycle、PC、
  位址空間與讀寫方向。優先使用 breakpoint、watchpoint、trace 與狀態差分定位第一個
  分歧，不以最後一張壞畫面倒推猜修。
- 未知或不支援的硬體操作預設失敗即關閉（fail-closed）：有界記錄 fault 並安全停止，
  不得越界讀寫、無限迴圈或靜默假裝成功。已知的 open-bus／no-op 行為除外。
- 相容性證據至少區分：`confirmed-hardware`、`confirmed-Bcan`、`MAME-derived`、
  `software-observed`、`strong-inference`、`hypothesis`、`unknown`。測試通過只證明列出的
  軟體與路徑，不自動升格成硬體真相。

### Save state 與衍生狀態

- Save state 必須在明確的指令／事件邊界擷取，涵蓋所有會影響未來輸出的可變狀態，
  包括 CPU 內部狀態、RAM、裝置暫存器、IRQ、DMA、timer、排程餘數與 latch。
- framebuffer、解碼 cache 等衍生狀態可不序列化，但載入後必須能確定重建。格式要有
  magic、版本、header size、ROM／BIOS 身分、payload 長度與完整性檢查；不相容或損壞
  預設拒絕載入，不允許部分套用。
- 載入必須具交易性：先完整驗證，再一次提交；失敗不得留下半套狀態。跨 ROM 載入
  預設拒絕，只有明確除錯選項才可放寬並醒目警告。

## 變更閘門

1. 先定義外部可觀察契約與證據等級，再改裝置模型。
2. 每個修正須附最小回歸測試，至少檢查受影響的 bus／IRQ／frame／audio／state 之一；
   同時抽測兩條使用不同驅動或硬體路徑的遊戲，避免單一遊戲特判。
3. 不把遊戲 PC、ROM hash 或標題名稱寫入核心來繞過硬體缺口。暫時探針只能由明確的
   debug 開關啟用，完成後移除或列入待辦。
4. 借鑑 MAME 時固定來源版本與檔案路徑，區分「規格參考」與「程式碼衍生」，遵守
   BSD-3-Clause 標示；若直接移植程式碼，不得聲稱重新實作。
5. 新增或修改狀態欄位時，同步檢查 reset、save／load、trace 與測試；裝置在冷開機、
   soft reset、load state 後都必須有定義良好的狀態。
6. 平台前端（SDL、Linux、未來 macOS）與模擬核心分離。平台移植不得改變硬體語意；
   headless 模式必須能執行核心回歸。

## 作業與交接

- 遵守上層共用主機衛生規則：分析、建置、測試與執行一律在有資源上限的 Docker
  容器內；原始輸入唯讀掛載，可寫容器指定目前 UID/GID，結束後檢查容器清理狀態。
- 開工先讀 `CONTEXT.md`、`WORKLIST.md`、最近驗證與 `git status`。現有未提交變更視為
  前任成果，未經確認不得回退。
- README 不保存逐輪命令或待辦；每輪歷程寫入唯一 `WORKLOG.md`。結束前記錄 HEAD、
  驗證命令與結果、未證實事項、下一個最小行動及 Docker 清理狀態。
